Before running:

 * run bin/setup.sh to add custom maven dependencies locally
 * Fill in the analytics user and pass
 * Set the correct start and end dates for analytics section
 * Choose an Analytics App name (used to create the analytics client)
 * Set the "end_date" for the twitter, facebook, and social reports
   end_date is for presentational purposes only, data is always current

Currently runs by just running Main.java after compiling.
Outputs to the scratch folder in the senate standard structure.

 * For various bad reasons you need to make the scratch and scratch/GA
   folders manually before running Main.java*

More documentation needed here and in the README

